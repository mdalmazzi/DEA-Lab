export class BoxPosition {
    top: number;
    left: number;  
    numero: number;

    constructor(top: number, left: number, numero: number )
    {
    this.top = top;
    this.left = left;
    this.numero = numero

    }

}